
# These seem to cause problems with rendering and stability
#export QT_QPA_PLATFORM="wayland;xcb"
#export GDK_BACKEND=wayland
#export CLUTTER_BACKEND=wayland

export PATH="$PATH:/home/crunge/.local/bin:/home/crunge/bin"

export XDG_DATA_DIRS="$HOME/.local/share/flatpak/exports/share:/var/lib/flatpak/exports/share:/usr/local/share:/usr/share"

export MOZ_ENABLE_WAYLAND=1
export CHROME_FLAGS="--enable-features=UseOzonePlatform --ozone-platform=wayland --auth-server-whitelist='*.lnt.ei.tum.de' $CHROME_FLAGS"
export BRAVE_FLAGS="--enable-features=UseOzonePlatform --ozone-platform=wayland --auth-server-whitelist='*.lnt.ei.tum.de' $BRAVE_FLAGS"
export AUTO_PROXY=http://pac.lrz.de

# Hail MATLAB
export _JAVA_AWT_WM_NONREPARENTING=1

SVDIR=~/service

source ~/.ssh/ssh-agent-env >/dev/null

alias adsinit='kinit ga42vof'

[ -f "/home/crunge/.ghcup/env" ] && source "/home/crunge/.ghcup/env" # ghcup-env
