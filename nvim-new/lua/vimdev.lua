
-- Prints all highlight groups of the token under the cursor
-- Could port to lua if ever needed
-- function! <SID>PrintSynStack()
--   if exists("*synstack")
--     echo map(synstack(line('.'), col('.')), 'synIDattr(v:val, "name")')
--   endif
-- endfunction

-- vim: ts=2 sts=2 sw=2 noet
